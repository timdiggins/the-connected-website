<h2>Recent activity</h2>

<div class="events">
    <ul>
     <% for event in @events %>
         <%= render :partial => "events/event", :locals => { :event => event } %>
     <% end %>
    </ul>

		<p class="more"><%= link_to "View all activity", events_url %></p>

</div>
