<h2>Recent discussion events</h2>
  <div class="events">
      <ul>
       <% for event in @events %>
           <%= render :partial => "events/event", :locals => { :event => event } %>
       <% end %>
      </ul>

	<p class="more"><%= link_to "View all events", events_url %></p>

</div>
