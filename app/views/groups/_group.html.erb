<div class="group grid_11 alpha omega">
    
  	  
  		<div class="grid_7 alpha">
  	  	<a href="<%= group_path(group) %>" class="title"><%= group %><span class="category"><%= h(group.category) %></span></a>
    	  <div class="profileText"><%= h(group.desc) %></div>
    	  <div class="clearRight"> </div>
  	  </div>
  	  <% if group.latest_image %>
    		<div class="grid_4 omega">
         <% group.images.latest3.each do |image| %>
				    <%= image_tag_for(image, :medium_square) %>
					<% end %>
        </div>
  		<% end %>
  		
	  </a>
</div>