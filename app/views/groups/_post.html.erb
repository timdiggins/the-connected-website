<a href="<%= post_path(post) %>" class="imageStripImage">
    <p><%= plain_text(post.detail) %></p>
		<div class="label" >
      <span class="title"><%= post.title %>.</span>
    </div>  
 </a>
      