<h1><%= @page_title = "Featured Postings only" %></h1>
		
<div id="sidebar">
	<p id="newContent"><%= link_to "Start a new posting", new_post_url %></p>
	
	
	<div id="subscribe">
	  <h2>Subscribe</h2>
	  <p><a href="<%= formatted_posts_url(:format => :rss) %>" class="rss">All postings</a></p>
	  <p><a href="<%= formatted_featured_posts_url(:format => :rss) %>" class="rss">Featured postings only</a></p>
	</div>
	
	<p class="featuredSelet">You are browsing featured postings. <%= link_to "Show all postings?", posts_url %></p>
	
</div>

<div id="main">
	<%= render :partial => 'posts/post_list' %>
</div>
<%= clear_this %>